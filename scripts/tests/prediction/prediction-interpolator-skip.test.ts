import { describe, test } from "bun:test";

describe("prediction interpolator skip local", () => {
  test.todo(
    "skipEntityIds prevents interpolation overriding local prediction",
    () => {},
  );
});
